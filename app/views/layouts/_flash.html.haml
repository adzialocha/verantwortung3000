.container

  - flash.each do |key, value|

    %div{:class => "alert #{alert_class_for(key)} alert-dismissible", :role => "alert"}
      %button.close{"data-dismiss" => "alert", :type => "button"}
        %span &times;

      = value
