.items-select

  = f.input id, as: :integer

  .items-select-cell-container.clearfix

    .items-select__cell.items-select__cell--default{ 'data-value': -1 }

      %h4 Doesnt exist
      %p Create a post on the black board

    - collection.each do |item|

      .items-select__cell{ 'data-disabled': false, 'data-value': item.id }

        = image_tag cover_thumb_url(item), class: 'img-responsive'

        %h4= item.title
        %p= item.description

  .items-select__create

    = f.input :title
    = f.input :description
