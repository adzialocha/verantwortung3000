.instrument

  = f.input :instrument_id, collection: Instrument.all

  = f.input :title
  = f.input :description

  = link_to_remove_association 'Remove Instrument', f, class: 'btn btn-danger',data: { confirm: 'Are you sure?' }, wrapper_class: 'instrument'
