.container

  = simple_form_for @production do |f|
    = f.input :title
    = f.input :description
    = f.button :submit

.container

  %p Unhappy?

  = button_to "Delete Production", production_path(@production), data: { confirm: "Are you sure?" }, method: :delete
